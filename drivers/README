drivers examples README
=======================

This directory has historically been the place for lirc kernel
module sources. Nowadays, it's just example code how to build
a userspace driver out-of-reee and a lirc kernel module example.
Here is also test tools such as irpipe, a kernel driver aimed
to replace the default driver while testing.
