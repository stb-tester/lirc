# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/tty[0-9]*
    driver:         pixelview
    id:             realmagic
    label:          REALmagic (bundled with Hollywood Plus DVD card)
    lircd_conf:     sigma_designs/realmagic.lircd.conf
    menu:           other_serial
    supports:       lirccode
