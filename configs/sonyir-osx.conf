# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         auto
    driver:         sonyir
    id:             sonyir-osx
    label:          Sony PCVA-IR5U IR Receiver (OSX version)
    lircd_conf:     sonyir.lircd.conf
    menu:           tv_card
    supports:       lirccode
    note:           This driver is for MacOSX only.
