# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/lirc*
    driver:         default
    id:             avermedia
    label:          AverMedia TV card (TVCapture, TVPhone) (card=6)
    lircd_conf:     avermedia/avermedia.lircd.conf
    lircmd_conf:    avermedia/avermedia.lircmd.conf
    menu:           tv_card
    modules:        ['lirc_dev']
    supports:       lirccode
