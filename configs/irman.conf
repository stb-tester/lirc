# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/tty[0-9]*
    driver:         irman
    id:             irman
    label:          Irman/UIR
    lircd_conf:     any
    menu:           other_serial
    supports:       timing
    note:           This driver has no transmit capabilities

