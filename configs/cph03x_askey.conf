# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/lirc*
    driver:         default
    id:             cph03x_askey
    label:          Askey  Magic TView CPH03x (card=1)
    lircd_conf:     cph03x/cph03x.lircd.conf
    menu:           tv_card
    modules:        ['lirc_dev', 'lirc_gpio']
    supports:       lirccode
