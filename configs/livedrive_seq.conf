# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/sequencer
    driver:         livedrive_seq
    id:             livedrive_seq
    label:          Creative  LiveDrive sequencer
    lircd_conf:     creative/livedrive.lircd.conf
    menu:           other
    supports:       lirccode
