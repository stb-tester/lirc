# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/tty[0-9]*
    driver:         mouseremote
    id:             mouseremote
    label:          X10  MouseRemote RF Receiver (Serial)
    lircd_conf:     x10/mouseremote.lircd.conf
    lircmd_conf:    x10/mouseremote.lircmd.conf
    menu:           other_serial
    supports:       lirccode
