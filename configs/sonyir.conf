# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/usb/hiddev*
    driver:         sonyir
    id:             sonyir
    label:          Sony PCVA-IR5U IR Receiver
    lircd_conf:     sonyir.lircd.conf
    menu:           tv_card
    supports:       lirccode
