# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/lirc*
    driver:         default
    id:             sa1100_hp
    label:          HP  iPAQ
    lircd_conf:     any
    menu:           pda
    modules:        ['lirc_dev', 'lirc_sir']
    supports:       timing
