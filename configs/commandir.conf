# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/ttyUSB*
    driver:         commandir
    id:             commandir
    label:          CommandIR Multi-IR Transceiver (userspace)
    lircd_conf:     hauppauge/hauppauge.lircd.conf
    lircmd_conf:    hauppauge/hauppauge.lircmd.conf
    menu:           usb
    supports:       timing
