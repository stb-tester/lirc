# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/lirc*
    driver:         default
    id:             chronos
    label:          Chronos  Video Shuttle II (card=35)
    lircd_conf:     chronos/chronos.lircd.conf
    menu:           tv_card
    modules:        ['lirc_dev', 'lirc_gpio']
    supports:       lirccode
