# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/i2c-0
    driver:         i2cuser
    id:             i2cuser
    label:          User-space I2C driver
    menu:           other
    supports:       bundled
