# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/ttyACM*
    driver:         irtoy
    id:             irtoy
    label:          Dangerous Prototypes irtoy device
    lircd_conf:     any
    menu:           usb
    supports:       timing
