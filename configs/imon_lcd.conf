# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/lirc*
    driver:         default
    id:             imon_lcd
    label:          Soundgraph  iMON IR/LCD
    lircd_conf:     imon/imon.lircd.conf
    menu:           usb
    modules:        ['lirc_dev', 'lirc_imon']
    supports:       Usually only lirccode
