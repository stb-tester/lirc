# This is a lirc configuration for a capture device.
# See README  for more.

config:
    device:         /dev/lirc*
    driver:         default
    id:             default
    label:          Default driver
    lircd_conf:     any
    menu:           default
    supports:       timing
