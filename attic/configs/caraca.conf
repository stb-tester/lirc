# This is a lirc configuration for a capture device.
# See README  for more.

config:
    driver:         caraca
    id:             caraca
    label:          CARACA
    menu:           other_serial
